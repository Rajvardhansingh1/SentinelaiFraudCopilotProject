[theme]
# "Verification teal" on deep slate — reads as a security/trust console,
# not the default Streamlit blue-on-white.
base = "dark"
primaryColor = "#2DD4BF"
backgroundColor = "#0F172A"
secondaryBackgroundColor = "#1E293B"
textColor = "#E2E8F0"
font = "sans serif"
